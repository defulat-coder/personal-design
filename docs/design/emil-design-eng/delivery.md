# 全站设计交付目标 · 2026-09-08

目标：安装并使用 emil-design-eng，先形成统一规范，再按每个页面／功能完成设计实现和端到端验证。用户最新约束及既有内容能力优先；桌面重点，零动画库。

技能安装：`.agents/skills/emil-design-eng/SKILL.md`，来源 https://github.com/emilkowalski/skills/tree/main/skills/emil-design-eng ，通过技能安装器 git 方式安装。设计规范先于实现写入根 DESIGN.md。

## 阶段

- [x] 安装技能，读取项目规范、页面、共享交互与内容数据接口。
- [x] 梳理页面和功能，先整理根 DESIGN.md。
- [x] 按规范实现各页面与共享功能。
- [x] 类型、Lint、单元检查与生产构建。
- [x] 端到端场景、双主题与减少动态效果验证，记录证据并交付。

## 页面与功能验收表

| 页面／功能 | 交付要求 | 结果 |
| --- | --- | --- |
| 全站外壳 | 品牌、主题、跳至内容；无重复导航或副标题 | 通过：1440px首页仅品牌与主题；内页返回首页；共享跳至内容与焦点样式复核 |
| 首页时间轴 | 日期顺序、真实预览、末端延伸、仅溢出显控制 | 通过：实际两产品、日期及末端延伸；1440px无翻页控件 |
| 首页拖动／键盘 | 拖动不误导航，键盘即时，端点正确 | 通过：800px溢出出现控制，方向键即时滚动；拖动214px回起点不误导航 |
| 两列表标题／分类 | 统一层级、中文分类、稳定位置 | 通过：1440/1280截图复核，中文分类与固定网格 |
| 搜索展开／关闭 | 按钮及 /、可中断，Esc／Enter／外部／Tab收起 | 通过：/、Esc、Enter、Tab、外部点击；键盘过渡0s |
| 即时搜索 | URL同步、查询可见、清除只清关键词、零结果 | 通过：button与黄金即时检索；清除关键词保留分类，空结果保留查询 |
| 布局主题筛选 | 深链有效，不显示主题下拉 | 通过：缺图详情主题链接筛出12条；非法分类／主题自动归一 |
| 布局预览 | 规整网格、停留放大、失败与缺图保留入口 | 通过：停留放大；网络拦截001缩略图显示兜底，仍能进入详情 |
| 灵感预览 | 视频默认静音循环、离屏暂停、海报／失败兜底 | 通过：可视播放、离屏暂停、减少动态效果暂停；海报与失败路径代码复核 |
| 灵感自动追加 | 按批追加，无手动加载／开关 | 通过：24→48自动追加，返回保留48件，无手动追加入口 |
| 布局详情 | PC高清阅读、分类主题、缺图恢复、相关内容 | 通过：002高清阅读、117缺图及同主题入口；高清失败可重试恢复 |
| 灵感单图详情 | 作品与作者原作入口、直接放大、真实说明 | 通过：Sketch logo单图、作者／分类／原作入口、直接放大；无空说明占位 |
| 灵感视频详情 | 控制可达、剩余视口、手动暂停保持 | 通过：realistic button控制位于900px视口内，点击原生暂停后保持 |
| 灵感多媒体 | 原生滚动、前后、计数、当前原媒体链接 | 通过：Product comps四图，End到4/4、首尾禁用、原图入口同步 |
| 相邻与返回 | 筛选顺序、URL、滚动、焦点、追加数量恢复 | 通过：两个产品筛选翻页与返回；灵感恢复48件及1334px滚动位置 |
| 灯箱 | 关闭、Tab圈定、Esc还焦、翻图、失败重试 | 通过：键盘即时开关、Tab圈定、Esc还焦、四图循环、失败重试恢复 |
| 404／页面错误 | 明确恢复入口、错误不丢当前地址 | 通过：实际404；临时throw触发根错误边界并重试，保留?q=gold；临时页已移除 |
| 双主题／减少动态 | 可读、焦点可见、无自动视频或空间动画 | 通过：首页／列表／详情双主题；减少动态效果暂停；390px详情无横向溢出 |

## 实现前审阅（Emil 格式）

| Before | After | Why |
| --- | --- | --- |
| 首页入场850–950ms、悬停450–800ms | 首页260ms、轻反馈140–200ms | 作品入口立即可用 |
| 灵感筛选／追加反复整墙650ms入场 | 结果即时、标题与媒体位置稳定 | 高频浏览不等待 |
| 搜索keyframe展开、hidden硬关 | 可中断transition开合，键盘即时 | 快速开关保持连续 |
| 键盘与指针同样平滑滚动 | 键盘即时，指针保留短反馈 | 匹配输入频率 |
| 图鉴缩略图网络失败无显示状态 | 原框内缺图说明，链接仍有效 | 容错也是完整体验 |
| 主按钮实现4px与规范胶囊冲突 | 统一主按钮胶囊及按压反馈 | 规范与实现一致 |

## 验证记录

- `pnpm lint`：通过，全站无警告。
- `pnpm --filter @personal-design/web typecheck`：通过。
- `pnpm --filter @personal-design/web exec node --experimental-strip-types --test lib/browse-context.test.mjs`：4/4通过。
- `pnpm build`：通过，525个静态页面（350条图鉴、170条灵感，复用各自页面模板）。
- `git diff --check`：通过。
- 浏览器使用本地 HTTPS 站点，覆盖1440×900、1280×900桌面；800px首页溢出和390px详情作基础回归。验证实际内容模板及关键状态，不声称逐一人工查看520条内容。
- [浏览器检查记录](evidence/browser-checks.json)保留原始探测及复核：4条早期探测受到导航采样时间、定位器自动滚动和原生媒体按键影响，均有明确的后续复核记录。
- 图片失败通过浏览器网络拦截真实复现，重试前解除；页面异常通过临时测试页面触发真实React错误边界，完成后删除测试页。没有修改内容包、数据库或原始图片。

## 截图与查看

本地预览：[Personal Design](https://personal-design.localhost/)。交付为项目内代码、规范与本地验证；本次没有执行远端部署。

| 页面／状态 | 截图 |
| --- | --- |
| 首页 | [亮色](evidence/home.png) · [深色](evidence/home-dark.png) |
| 两个集合 | [灵感集](evidence/muse.png) · [布局参考](evidence/layouts.png) · [1280深色](evidence/layouts-dark-1280.png) |
| 搜索 | [搜索结果](evidence/muse-search.png) · [空结果](evidence/empty-search.png) |
| 图鉴阅读 | [详情](evidence/layout-detail.png) · [深色](evidence/detail-dark.png) · [缺图](evidence/layout-missing.png) |
| 灵感阅读 | [单图](evidence/muse-single.png) · [视频](evidence/muse-video.png) · [多图](evidence/muse-multiple.png) |
| 放大与恢复 | [灯箱](evidence/lightbox.png) · [预览失败](evidence/preview-failure.png) · [高清失败](evidence/detail-failure.png) |
| 页面恢复 | [404](evidence/404.png) · [错误边界](evidence/page-error.png) |

技能已可直接按项目内文件使用；后续新任务如尚未自动发现，可重启Codex刷新技能目录。

## 后续独立复审

2026-09-08—09 使用三个独立子代理完成多视角审计，修复5项常规路径之外的问题；后续结果与验证范围以[多视角复审](multi-review.md)为准。
